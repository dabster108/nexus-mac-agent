"""Stable contract identifiers for harness artifacts."""

from __future__ import annotations

#: JSON envelope schema written to ``results/<run_name>.json``.
RUN_SCHEMA = "nexus-evals/v1"

#: Package / harness identity (matches ``pyproject.toml`` name).
HARNESS_NAME = "nexus-evals"
